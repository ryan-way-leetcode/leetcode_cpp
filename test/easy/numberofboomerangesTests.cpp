#include <gtest/gtest.h>
#include "../../../include/easy/numberofboomerangs.hpp"


TEST(NumberOfBoomerangs, Example)
{
  vector<vector<int>> in = {
    { 0, 0 },
    { 1, 0 },
    { 2, 0 },
  };
  int exp = 2;
  NumberOfBoomerangs::Solution s = NumberOfBoomerangs::Solution();

  EXPECT_EQ(s.numberOfBoomerangs(in), 2);
}

TEST(NumberOfBoomerangs, Example)
{
  vector<vector<int>> in = {
    { 0, 0 },
    { 1, 0 },
    { 2, 0 },
  };
  int exp = 2;
  NumberOfBoomerangs::Solution s = NumberOfBoomerangs::Solution();

  EXPECT_EQ(s.numberOfBoomerangs(in), 2);
}

TEST(NumberOfBoomerangs, Example)
{
  vector<vector<int>> in = {
    { 0, 0 },
    { 1, 0 },
    { 2, 0 },
  };
  int exp = 2;
  NumberOfBoomerangs::Solution s = NumberOfBoomerangs::Solution();

  EXPECT_EQ(s.numberOfBoomerangs(in), 2);
}

TEST(NumberOfBoomerangs, Example)
{
  vector<vector<int>> in = {
    { 0, 0 },
    { 1, 0 },
    { 2, 0 },
  };
  int exp = 2;
  NumberOfBoomerangs::Solution s = NumberOfBoomerangs::Solution();

  EXPECT_EQ(s.numberOfBoomerangs(in), 2);
}

TEST(NumberOfBoomerangs, Example)
{
  vector<vector<int>> in = {
    { 0, 0 },
    { 1, 0 },
    { 2, 0 },
  };
  int exp = 2;
  NumberOfBoomerangs::Solution s = NumberOfBoomerangs::Solution();

  EXPECT_EQ(s.numberOfBoomerangs(in), 2);
}
