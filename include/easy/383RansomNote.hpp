#ifndef RANSOMNOTE
#define RANSOMNOTE

#include <string>
#include <map>
#include <algorithm>

using namespace std;

namespace RansomNote {
  
  class Solution {
    public:
      bool canConstruct(string ransomNote, string magazine);
  };

}
#endif
