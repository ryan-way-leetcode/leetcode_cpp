#ifndef COURSESCHEDULE
#define COURSESCHEDULE

#include <vector>
#include <queue>

using namespace std;

namespace CourseSchedule {
  
  class Solution {
    public:
      bool canFinish(int numCourses, vector<vector<int>>& prerequisites);
  };

}
#endif
