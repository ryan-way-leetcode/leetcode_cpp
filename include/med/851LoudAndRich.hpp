#ifndef LOUDANDRICH
#define LOUDANDRICH

#include <vector>
#include <stack>

using namespace std;

namespace LoudAndRich {
  
  class Solution {
    public:
      vector<int> loudAndRich(vector<vector<int>>& richer, vector<int>& quiet);
  };

}
#endif
